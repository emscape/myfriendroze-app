# Working notes (per CLAUDE.md — not user-facing)
docs/

# Secrets — never commit
secrets/
*serviceAccountKey.json
*-service-account.json

# Firebase client config (regenerate per-environment instead of committing)
**/google-services.json
**/GoogleService-Info.plist

# Firebase CLI local deploy cache (added once Hosting was wired up for the
# admin web app — see admin-app-web-hosting memory)
.firebase/

# Flutter web build output (regenerated via `flutter build web`)
**/build/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
.env
.env.local
